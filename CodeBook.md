Firstly both `test` and `train data sets are joined by row to give three combined data sets. These are stored in the variables
 `x.data`: Combined rows from X_train and X_test data files.
 `y.data`: Combined rows from Y_train and Y_test data files.
 `subject.data`: Combined rows from subject_train and subject_test data files.
